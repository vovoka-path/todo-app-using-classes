// export const createDOMElement = (html: string): HTMLElement | null => {
//   const container = document.createElement('div');
//   container.innerHTML = html;

//   if (container.firstChild instanceof HTMLElement) return container.firstChild;

//   return null;
// };
